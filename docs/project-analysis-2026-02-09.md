# Анализ проекта reestr-mkd - 9 февраля 2026

## 📊 Общее состояние проекта

### ✅ Статус: В разработке (миграция на PostgreSQL)

**Назначение:** Web-приложение для систематизированной инвентаризации жилых комплексов с пошаговым workflow процессом.

**Технологический стек:**
- Frontend: React 18 + Vite 7 + Tailwind CSS 3
- State Management: TanStack React Query 5
- Backend (DEV): Supabase (PostgreSQL + REST API)
- Валидация: Zod 4
- Тестирование: Node.js test runner

---

## 🏗️ Архитектура проекта

### 1. Трехслойная модель ProjectContext

Система управления состоянием разделена на независимые слои:

#### Data Layer (`useProjectDataLayer`)
- ✅ Объединение серверных данных и локальных изменений
- ✅ Вычисление режима readonly по роли и статусу
- ✅ Формирование валидационного snapshot

#### Sync Layer (`useProjectSyncLayer`)
- ✅ Очередь сохранений с debounce + manual flush
- ✅ Буферизация изменений
- ✅ Batch-операции с БД
- ✅ Координация refetch

#### Workflow Layer (`useProjectWorkflowLayer`)
- ✅ Переходы между шагами и статусами
- ✅ Генерация событий истории
- ✅ Контроль этапов и валидация

### 2. Workflow процесс

**17 шагов** (индексы 0-16), разделенных на **4 этапа**:

- **Этап 1** (шаги 0-5): Паспорт и состав комплекса
- **Этап 2** (шаги 6-8): Инвентаризация и нумерация
- **Этап 3** (шаги 9-11): Реестры помещений
- **Этап 4** (шаги 12-16): Интеграция и сводные отчеты

**Статусы заявки:**
- `NEW` → `DRAFT` → `REVIEW` → `APPROVED` / `REJECTED` → `INTEGRATION` → `COMPLETED`

**Роли:**
- `admin` — полный доступ
- `controller` — проверка (readonly на формах)
- `technician` — редактирование в разрешенных статусах

✅ **State machine тесты:** 7/7 пройдено
✅ **UJ-identifier тесты:** 15/15 пройдено

---

## 💾 База данных

### Схема PostgreSQL

**Файл:** `db/reset_schema.sql`

**Основные контуры:**

#### 1. Project/Application workflow
- `projects` — метаданные ЖК (с `uj_code`)
- `applications` — заявки с workflow
- `application_steps` — прогресс по шагам
- `application_history` — журнал событий

#### 2. Состав комплекса
- `buildings` — здания (с `building_code`)
- `building_blocks` — блоки зданий
- `block_construction` — конструктив
- `block_engineering` — инженерные системы
- `basements` — подвальные уровни

#### 3. Инвентаризация
- `floors` — этажи
- `entrances` — подъезды
- `entrance_matrix` — матрица этаж/подъезд
- `units` — помещения (с `unit_code`)
- `rooms` — экспликация
- `common_areas` — МОП

#### 4. Справочники (`dict_*`)
- Статусы, типы конструкций, материалы, системы
- Типы помещений, комнат, МОП
- Системные пользователи

**Ключевые особенности:**
- ✅ UUID primary keys
- ✅ Уникальности для idempotent upsert
- ✅ RLS включен (DEV: full access)
- ✅ JSONB для `integration_data`
- ✅ Cascading deletes

---

## 🔐 Система UJ-идентификаторов

### Реализация: ✅ Завершена (v2.0.0)

**Трёхуровневая иерархия:**

#### Уровень I: Проект
- Формат: `UJ000000` (6 цифр)
- Префикс: `UJ` (Uy Joy)
- Поле БД: `projects.uj_code`
- Пример: `UJ000001`

#### Уровень II: Здание
- Формат: `ZD00` (где D — тип)
- Префиксы:
  - `ZR` — Residential (одноблочный жилой)
  - `ZM` — Multi-block (многоблочный жилой)
  - `ZP` — Parking (паркинг)
  - `ZI` — Infrastructure (инфраструктура)
- Поле БД: `buildings.building_code`
- Пример: `ZR01`, `ZM02`

#### Уровень III: Помещение
- Формат: `EL000` (где L — тип)
- Префиксы:
  - `EF` — Flat (квартира)
  - `EO` — Office/Commercial (коммерция)
  - `EP` — Parking (машиноместо)
- Поле БД: `units.unit_code`
- Пример: `EF001`, `EO042`

### Полные идентификаторы
```
UJ000001-ZR01-EF001  // Квартира в жилом доме
UJ000001-ZP01-EP123  // Машиноместо в паркинге
UJ000042-ZI01-EO005  // Коммерция в инфраструктуре
```

### Реализованные компоненты:

✅ **IdentifierBadge** — переиспользуемый UI компонент
✅ **ApplicationsDashboard** — UJ-код в списке проектов + поиск
✅ **Sidebar** — UJ-код проекта в блоке "Объект"
✅ **WorkflowBar** — UJ-код рядом с текущей задачей
✅ **PassportEditor** — полный идентификатор `UJ000001`
✅ **CompositionEditor** — полный идентификатор здания `UJ000001-ZR01`
✅ **ConfigHeader** — полный идентификатор здания `UJ000001-ZR01`
✅ **ApartmentsRegistry** — полный идентификатор квартиры `UJ000001-ZR01-EF001`
✅ **CommercialRegistry** — полный идентификатор коммерции `UJ000001-ZM02-EO001`
✅ **ParkingRegistry** — полный идентификатор паркинга `UJ000001-ZP01-EP001`

---

## ⚠️ НЕРЕШЕННЫЕ ВОПРОСЫ

### 1. ❌ IntegrationBuildings — отсутствуют UJ-идентификаторы

**Проблема:** В компоненте `IntegrationBuildings.jsx` не отображаются:
- UJ-код проекта
- Building-код здания
- Полный идентификатор `UJ000001-ZR01`

**Где необходимо добавить:**
```jsx
// Строка 246-251: В колонке "Объект"
<td className="px-6 py-4">
  <div className="font-bold text-slate-800 text-sm mb-0.5">{item.label}</div>
  {/* ДОБАВИТЬ: Полный идентификатор */}
  {item.buildingCode && projectContext.complexInfo?.ujCode && (
    <IdentifierBadge 
      code={formatFullIdentifier(
        projectContext.complexInfo.ujCode, 
        item.buildingCode
      )}
      type="building"
      variant="compact"
    />
  )}
</td>
```

**Обоснование:**
- Критично для интеграции с УЗКАД
- Необходимо для отслеживания статуса регистрации
- Единообразие с другими компонентами

---

### 2. ❌ IntegrationUnits — отсутствуют UJ-идентификаторы

**Проблема:** В компоненте `IntegrationUnits.jsx` не отображаются:
- UJ-код проекта
- Building-код здания
- Unit-код помещения
- Полный идентификатор `UJ000001-ZR01-EF001`

**Где необходимо добавить:**
```jsx
// Строка 325-326: В колонке "Номер / Имя"
<td className="px-6 py-4">
  <div className="font-black text-slate-700">{item.number}</div>
  {/* ДОБАВИТЬ: Полный идентификатор */}
  {item.unitCode && (
    <div className="text-[9px] font-mono font-bold text-slate-500 mt-0.5">
      {formatFullIdentifier(
        projectContext.complexInfo?.ujCode,
        item.buildingCode, // Нужно добавить в данные из API
        item.unitCode
      )}
    </div>
  )}
</td>
```

**Обоснование:**
- Критично для интеграции с внешними системами
- Полный идентификатор для отправки в УЗКАД
- Отслеживание статуса регистрации помещений

---

### 3. ⚠️ API getProjectFullRegistry — отсутствует building_code

**Проблема:** В `IntegrationUnits` используется `fullRegistry` из API, но:
- Нет `buildingCode` для каждого unit
- Нет `buildingId` для каждого unit
- Невозможно отобразить полный идентификатор

**Необходимые изменения в API:**

```javascript
// src/lib/api/registry-api.js → getProjectFullRegistry
// Добавить JOIN для получения building_code через floor -> block -> building

const { data: unitsData } = await supabase
  .from('units')
  .select(`
    *,
    floor:floors!inner(
      id,
      block:building_blocks!inner(
        id,
        building:buildings!inner(
          id,
          building_code
        )
      )
    )
  `)
  .eq('floors.building_blocks.buildings.project_id', projectId);

// Маппинг результата для добавления buildingCode и buildingId
const enrichedUnits = unitsData.map(u => ({
  ...u,
  buildingId: u.floor?.block?.building?.id,
  buildingCode: u.floor?.block?.building?.building_code,
}));
```

---

### 4. ⚠️ Цветовая схема в IntegrationUnits

**Проблема:** В таблице `IntegrationUnits` используется универсальная цветовая схема для всех типов помещений.

**Рекомендация:** Использовать дифференцированную цветовую схему:
- **Квартиры** (EF) → синий (`bg-blue-100 text-blue-600`)
- **Коммерция** (EO) → зеленый (`bg-emerald-100 text-emerald-600`)
- **Паркинг** (EP) → индиго (`bg-indigo-100 text-indigo-600`)

---

### 5. ℹ️ DEV конфигурация Supabase

**Текущее состояние:**
- ✅ `.env.example` содержит открытые credentials Supabase
- ✅ RLS политики настроены с full-access для `anon`/`authenticated`

**Примечание:**
- ✅ Приемлемо для DEV/TEST окружения
- ⚠️ Для production необходимы строгие RLS политики
- ⚠️ Добавить проверку `scope_id` для изоляции данных

---

## 📝 Рекомендации по приоритетам

### Высокий приоритет (критично для интеграции):

1. **✅ Добавить UJ-идентификаторы в IntegrationBuildings**
   - Трудозатраты: ~30 минут
   - Блокирует: Интеграция с УЗКАД (шаг 12)

2. **✅ Добавить UJ-идентификаторы в IntegrationUnits**
   - Трудозатраты: ~1 час
   - Блокирует: Интеграция с УЗКАД (шаг 13)

3. **✅ Обновить API getProjectFullRegistry**
   - Трудозатраты: ~1 час
   - Блокирует: Отображение полных идентификаторов в реестрах

### Средний приоритет:

4. **Обновить цветовую схему в IntegrationUnits**
   - Трудозатраты: ~15 минут
   - Улучшает: UX и визуальную дифференциацию

5. **Добавить unit-тесты для IntegrationBuildings/Units**
   - Трудозатраты: ~1 час
   - Повышает: Надёжность интеграционных шагов

### Низкий приоритет:

6. **Подготовка к production (RLS, security)**
   - Трудозатраты: ~2-3 дня
   - Критично для: Production deployment

---

## 🧪 Тестирование

### ✅ Workflow tests: 7/7 пройдено

```bash
npm run test:smoke
```

Покрытие:
- ✅ Обычный переход между шагами
- ✅ Граница этапа → REVIEW
- ✅ Граница перед интеграцией → REVIEW (приоритет)
- ✅ Последний шаг → COMPLETED
- ✅ Rollback из REVIEW → DRAFT
- ✅ Review approve
- ✅ Review reject

### ✅ UJ-identifier tests: 15/15 пройдено

```bash
node --test tests/uj-identifier.test.mjs
```

Покрытие:
- ✅ Генерация кодов всех уровней
- ✅ Валидация форматов
- ✅ Определение префиксов
- ✅ Форматирование и парсинг полных идентификаторов
- ✅ Извлечение номеров и вычисление следующих значений
- ✅ Интеграционные сценарии
- ✅ Нумерация в пределах типов

---

## 📚 Документация

### Полнота: ✅ Отлично

Вся критическая документация присутствует:

- ✅ `README.md` — обзор проекта и quick start
- ✅ `docs/project-full-architecture.md` — полная архитектура
- ✅ `docs/project-full-db-schema.md` — схема БД
- ✅ `docs/project-full-workflow.md` — workflow и state machine
- ✅ `docs/project-full-code-structure.md` — структура кода
- ✅ `docs/uj-identifiers.md` — система идентификаторов
- ✅ `docs/uj-full-identifiers-display.md` — отображение в UI
- ✅ `docs/uj-ui-implementation-summary.md` — итоги реализации
- ✅ `docs/db-reset-runbook.md` — инструкция по сбросу БД
- ✅ `docs/upsert-on-conflict.md` — стратегия upsert операций
- ✅ `CHANGELOG-UJ-IDENTIFIERS.md` — changelog системы идентификаторов

---

## 🎯 Выводы

### Общая оценка: ✅ Хорошо

**Сильные стороны:**
1. ✅ Хорошо продуманная архитектура (трехслойная модель)
2. ✅ Полная система UJ-идентификаторов (v2.0.0)
3. ✅ Workflow state machine с тестами
4. ✅ Подробная документация
5. ✅ Готовая схема БД с миграциями
6. ✅ Единообразный UI с переиспользуемыми компонентами

**Области для улучшения:**
1. ⚠️ Завершить интеграцию UJ-идентификаторов в IntegrationBuildings/Units
2. ⚠️ Обновить API для передачи building_code в реестры
3. ℹ️ Подготовить production security (RLS политики)
4. ℹ️ Добавить больше unit-тестов для компонентов
5. ℹ️ Документировать процесс deployment

### Статус готовности к DEV: ✅ 95%

**Блокеры для завершения DEV:**
- Отсутствие UJ-идентификаторов в IntegrationBuildings (критично)
- Отсутствие UJ-идентификаторов в IntegrationUnits (критично)
- API не передаёт building_code для units (критично)

**Ориентировочное время на устранение блокеров: ~2-3 часа**

---

## 📋 Action Items

### Немедленно:

- [ ] Добавить IdentifierBadge в IntegrationBuildings
- [ ] Добавить IdentifierBadge в IntegrationUnits
- [ ] Обновить API getProjectFullRegistry для возврата building_code

### В ближайшее время:

- [ ] Обновить цветовую схему в IntegrationUnits
- [ ] Добавить unit-тесты для интеграционных компонентов
- [ ] Проверить все компоненты на наличие UJ-идентификаторов

### Долгосрочно:

- [ ] Подготовить RLS политики для production
- [ ] Настроить CI/CD pipeline
- [ ] Добавить мониторинг и логирование
- [ ] Документировать deployment процесс

---

**Дата анализа:** 9 февраля 2026  
**Аналитик:** AI Assistant  
**Версия проекта:** 1.0.1  
**Статус:** В разработке (PostgreSQL migration)
