# Руководство по очистке неиспользуемого кода

**Дата:** 9 февраля 2026  
**Цель:** Удаление неиспользуемых файлов для улучшения maintainability проекта

---

## 📋 Файлы к удалению

### 1. ProjectsDashboard.jsx

**Путь:** `src/components/ProjectsDashboard.jsx`

**Причина удаления:**
- Не используется в текущей версии приложения
- Функциональность полностью заменена на `ApplicationsDashboard.jsx`
- Содержит устаревшую логику с demo-данными

**Последствия удаления:**
- ✅ Нет зависимых файлов
- ✅ Не импортируется ни в одном активном компоненте
- ✅ Упоминается только в документации (обновлена)

---

### 2. ProjectsPage.jsx

**Путь:** `src/components/ProjectsPage.jsx`

**Причина удаления:**
- Не используется в React Router
- Функциональность дублируется в `ApplicationsDashboard`
- Нет маршрутов, ведущих к этому компоненту

**Последствия удаления:**
- ✅ Нет зависимых файлов
- ✅ Не используется в навигации
- ✅ Упоминается только в документации (обновлена)

---

## 🚀 Шаги по очистке

### Шаг 1: Создать backup (опционально)

```bash
# Создать ветку для очистки
git checkout -b cleanup/remove-unused-components

# Или создать резервную копию файлов
mkdir -p .backup
cp src/components/ProjectsDashboard.jsx .backup/
cp src/components/ProjectsPage.jsx .backup/
```

### Шаг 2: Удалить файлы

```bash
# Удалить неиспользуемые компоненты
rm src/components/ProjectsDashboard.jsx
rm src/components/ProjectsPage.jsx
```

### Шаг 3: Проверить, что проект работает

```bash
# Установить зависимости (если не установлены)
npm install

# Проверить линтер
npm run lint

# Запустить dev server
npm run dev

# В отдельном терминале - запустить тесты
npm run test:smoke

# Собрать production build
npm run build
```

### Шаг 4: Проверить приложение вручную

1. Открыть http://localhost:5173
2. Залогиниться
3. Проверить дашборд проектов
4. Открыть любой проект
5. Пройтись по нескольким шагам
6. Вернуться на дашборд

**Ожидаемый результат:** Все работает без ошибок в консоли.

### Шаг 5: Закоммитить изменения

```bash
git add .
git commit -m "refactor: remove unused components (ProjectsDashboard, ProjectsPage)

- Removed src/components/ProjectsDashboard.jsx (replaced by ApplicationsDashboard)
- Removed src/components/ProjectsPage.jsx (functionality moved to ApplicationsDashboard)
- Updated documentation to reflect changes
- See docs/project-recommendations.md for details"
```

---

## ✅ Чеклист проверки

После удаления убедитесь:

- [ ] `npm run dev` запускается без ошибок
- [ ] `npm run build` завершается успешно
- [ ] `npm run lint` не выдает новых ошибок
- [ ] `npm run test:smoke` проходит все тесты
- [ ] Приложение открывается в браузере
- [ ] Дашборд проектов отображается корректно
- [ ] Можно создать новый проект (если есть эта функция)
- [ ] Можно открыть существующий проект
- [ ] Нет ошибок в консоли браузера
- [ ] Документация обновлена

---

## 🔍 Где искать проблемы, если что-то сломалось

### Ошибки импорта

```
Error: Cannot find module './components/ProjectsDashboard'
```

**Решение:** Проверьте, не осталось ли импортов в:
- `src/App.jsx`
- `src/components/**/*.jsx`
- Файлы тестов

### Ошибки при сборке

```
Build failed: Module not found
```

**Решение:**
1. Очистить кэш Vite: `rm -rf node_modules/.vite`
2. Пересобрать: `npm run build`

### Ошибки в тестах

```
ReferenceError: ProjectsDashboard is not defined
```

**Решение:** Проверьте файлы тестов на наличие ссылок на удаленные компоненты.

---

## 📊 Ожидаемые результаты

### Метрики до очистки

- Файлов компонентов: 52
- Строк кода компонентов: ~10,000+
- Размер bundle (оценка): ~600KB (gzipped)

### Метрики после очистки

- Файлов компонентов: 50 (-2)
- Строк кода компонентов: ~9,600 (-400 строк, ~4%)
- Размер bundle (оценка): ~595KB (незначительное уменьшение)

### Качественные улучшения

- ✅ Меньше «мертвого кода» для поддержки
- ✅ Упрощенная навигация по структуре проекта
- ✅ Меньше confusion при поиске «правильного» компонента дашборда
- ✅ Актуальная документация

---

## 🔄 Откат изменений (если нужно)

Если после удаления обнаружились проблемы:

### Вариант 1: Восстановить из backup

```bash
cp .backup/ProjectsDashboard.jsx src/components/
cp .backup/ProjectsPage.jsx src/components/
```

### Вариант 2: Откатить коммит

```bash
# Посмотреть последние коммиты
git log --oneline -5

# Откатить последний коммит (мягкий откат, файлы остаются в рабочей директории)
git reset --soft HEAD~1

# Или откатить с удалением изменений
git reset --hard HEAD~1
```

### Вариант 3: Восстановить конкретные файлы из истории

```bash
# Найти последний коммит, где файлы существовали
git log -- src/components/ProjectsDashboard.jsx

# Восстановить из конкретного коммита
git checkout <commit-hash> -- src/components/ProjectsDashboard.jsx
git checkout <commit-hash> -- src/components/ProjectsPage.jsx
```

---

## 📝 Заметки

### Почему эти файлы стали неиспользуемыми?

Скорее всего, в процессе разработки был создан более совершенный компонент `ApplicationsDashboard`, который объединил функциональность списка проектов и заявок в одном интерфейсе. Старые компоненты не были удалены сразу, возможно, для обратной совместимости или на случай возврата к прежнему дизайну.

### Безопасно ли удалять?

✅ **Да, абсолютно безопасно**, потому что:
1. Файлы не импортируются нигде в коде
2. Нет маршрутов, ведущих к этим компонентам
3. Git-история сохранит файлы, если потребуется вернуться к ним
4. Тесты не зависят от этих файлов

### Что если понадобятся в будущем?

Если когда-нибудь потребуется вернуть эту функциональность:
1. Файлы всегда можно восстановить из Git-истории
2. Логика уже реализована в `ApplicationsDashboard`
3. Можно использовать как reference implementation

---

## ✨ Дополнительные улучшения (опционально)

После успешной очистки можно рассмотреть:

### 1. Удалить неиспользуемые импорты

```bash
# Установить инструмент для поиска неиспользуемых зависимостей
npx depcheck
```

### 2. Проверить неиспользуемые CSS классы

```bash
# Установить PurgeCSS (если не используется через Tailwind)
npm install -D purgecss
```

### 3. Оптимизировать импорты

Проверьте, не импортируются ли целые библиотеки там, где можно импортировать только нужные части:

```jsx
// Плохо
import _ from 'lodash';

// Хорошо
import { debounce } from 'lodash';
```

---

**Удачной очистки!** 🧹

При возникновении вопросов обращайтесь к документации:
- `docs/project-recommendations.md` — полный список рекомендаций
- `docs/project-full-code-structure.md` — структура кода
- `docs/project-full-architecture.md` — архитектура проекта

---

**Составлено:** AI Assistant  
**Дата:** 9 февраля 2026
