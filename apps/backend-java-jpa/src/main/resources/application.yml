server:
  port: ${PORT:8787}
  address: ${HOST:0.0.0.0}

spring:
  application:
    name: reestr-mkd-backend-java-jpa
  datasource:
    url: "jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?prepareThreshold=0&stringtype=unspecified"
    username: "postgres.rhfllxqyjgvlodnxlgvz"
    password: "Samsung0477061-"
    # ВОТ ЭТОТ БЛОК КРИТИЧЕСКИ ВАЖЕН ДЛЯ SUPABASE:
    hikari:
      data-source-properties:
        prepareThreshold: 0
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

app:
  auth-mode: ${AUTH_MODE:dev}
  jwt-secret: "my_super_secret_dev_key_12345!@#"
  runtime-env: ${RUNTIME_ENV:${APP_ENV:${NODE_ENV:dev}}}