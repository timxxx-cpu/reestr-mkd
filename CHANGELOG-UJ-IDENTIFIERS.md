# Changelog: UJ-идентификаторы

## [2.0.0] - 2026-02-09 - Полные иерархические идентификаторы

### ✨ Обновлено

#### Отображение полных идентификаторов во всех компонентах

**Формат отображения по уровням:**
- **Уровень 1 (Проект):** `UJ000001`
- **Уровень 2 (Здание):** `UJ000001-ZR01`
- **Уровень 3 (Помещение):** `UJ000001-ZR01-EF001`

#### Обновлённые компоненты:

1. **IdentifierBadge.jsx** — Добавлен `FullIdentifierCompact`
   - Отображение полного кода в одну строку
   - Поддержка копирования по клику
   - Три размера: compact, default, large

2. **CompositionEditor** → `UJ000001-ZR01`
   - Полный идентификатор здания вместо короткого кода
   - Единообразие с остальными компонентами

3. **ConfigHeader** → `UJ000001-ZR01`
   - Полный код в заголовке конфигуратора
   - Контекст при работе с зданием

4. **ApartmentsRegistry** → `UJ000001-ZR01-EF001`
   - Полная иерархия для каждой квартиры
   - Удобно для интеграции

5. **CommercialRegistry** → `UJ000001-ZM02-EO001`
   - Полная иерархия для коммерции
   - Зелёная цветовая схема

6. **ParkingRegistry** → `UJ000001-ZP01-EP001`
   - Полная иерархия для машиномест
   - Синяя цветовая схема

7. **PassportEditor** → `UJ000001`
   - Использование FullIdentifierCompact
   - Единообразие API

#### API обновления:

- `getProjectFullRegistry` — Добавлен возврат `building_code` и `unit_code`
- Обогащение данных в реестрах через `buildingCode` из маппинга

### 📚 Документация

Созданы документы:
- `docs/uj-full-identifiers-display.md` — Подробное описание отображения

---

## [1.0.0] - 2026-02-09 - Базовая система идентификаторов

### ✨ Добавлено

#### Система трёхуровневых UJ-идентификаторов

**Структура:**
- **Уровень I:** Проект — `UJ000000` (6 цифр)
- **Уровень II:** Здание — `ZR01`, `ZM01`, `ZP01`, `ZI01` (по типу)
- **Уровень III:** Помещение — `EF001`, `EO001`, `EP001` (по типу)

#### Схема БД:

```sql
-- Проекты
ALTER TABLE projects ADD COLUMN uj_code text;
CREATE UNIQUE INDEX idx_projects_uj_code ON projects(uj_code);

-- Здания
ALTER TABLE buildings ADD COLUMN building_code text;
CREATE UNIQUE INDEX idx_buildings_code ON buildings(building_code);

-- Помещения
ALTER TABLE units ADD COLUMN unit_code text;
CREATE UNIQUE INDEX idx_units_code ON units(unit_code);
```

#### Утилиты генерации:

**Файл:** `src/lib/uj-identifier.js`

Функции:
- `generateProjectCode(number)` → `UJ000000`
- `generateBuildingCode(prefix, number)` → `ZD00`
- `generateUnitCode(prefix, number)` → `EL000`
- `getBuildingPrefix(category, hasMultipleBlocks)` → `ZR|ZM|ZP|ZI`
- `getUnitPrefix(unitType)` → `EF|EO|EP`
- `formatFullIdentifier(...)` → `UJ000000-ZD00-EL000`
- `parseIdentifier(...)` → `{ projectCode, buildingCode, unitCode }`
- Валидация, извлечение номеров, вычисление следующих значений

#### Автоматическая генерация в API:

**Файл:** `src/lib/api-service.js`

- `createProjectFromApplication` — генерация UJ-кода при создании проекта
- `createBuilding` — генерация building_code с учётом типа и блоков
- `upsertUnit` — генерация unit_code с учётом типа помещения

Нумерация **в пределах типа** внутри родительской сущности.

#### Маппинг данных:

**Файл:** `src/lib/db-mappers.js`

- `mapProjectAggregate` — добавлено `ujCode`
- `mapBuildingFromDB` — добавлено `buildingCode`
- `mapUnitFromDB` — добавлено `unitCode`

#### UI компоненты:

**Файл:** `src/components/ui/IdentifierBadge.jsx`

Создан переиспользуемый компонент:
- Три типа: project, building, unit
- Три размера: compact, default, large
- Копирование по клику
- Анимации и стили

**Интеграция в компоненты:**
1. ApplicationsDashboard — UJ-код в списке проектов + поиск
2. Sidebar — UJ-код в информации об объекте
3. WorkflowBar — UJ-код рядом с текущей задачей
4. PassportEditor — UJ-код в заголовке
5. CompositionEditor — Building-код рядом с названием (обновлено до полного)
6. ConfigHeader — Building-код в шапке (обновлено до полного)
7. ApartmentsRegistry — Unit-код под номером (обновлено до полного)
8. CommercialRegistry — Unit-код для коммерции (обновлено до полного)
9. ParkingRegistry — Unit-код для паркинга (обновлено до полного)

### 🧪 Тестирование

**Файл:** `tests/uj-identifier.test.mjs`

- 15 unit-тестов
- ✅ 15/15 успешно пройдены
- Покрытие: генерация, валидация, сценарии использования

### 📚 Документация

Созданы документы:
1. `docs/uj-identifiers.md` — Полное описание системы
2. `docs/uj-ui-placement-proposal.md` — Предложения по размещению
3. `docs/uj-ui-implementation-summary.md` — Итоги первой фазы
4. `docs/uj-full-identifiers-display.md` — Отображение полных кодов

---

## Коммиты

### v2.0.0 - Полные идентификаторы
- `289946b` - Обновлены все UI компоненты для полных идентификаторов
- `043b071` - Обновлён PassportEditor для единообразия
- `2063398` - Добавлена документация по полным идентификаторам

### v1.0.0 - Базовая система
- `07dcc3f` - Добавлена система трёхуровневых UJ-идентификаторов
- `535a792` - Добавлены тесты для UJ-идентификаторов
- `5ddb962` - Добавлены UJ-идентификаторы во все ключевые UI
- `88a7707` - Добавлен итоговый документ по реализации

---

## Миграция

### Для существующих проектов:

Если в БД уже есть проекты без кодов, выполните:

```sql
-- 1. Обновите схему
\i db/reset_schema.sql

-- 2. Или добавьте поля вручную:
ALTER TABLE projects ADD COLUMN IF NOT EXISTS uj_code text;
ALTER TABLE buildings ADD COLUMN IF NOT EXISTS building_code text;
ALTER TABLE units ADD COLUMN IF NOT EXISTS unit_code text;

CREATE UNIQUE INDEX IF NOT EXISTS idx_projects_uj_code 
  ON projects(uj_code) WHERE uj_code IS NOT NULL;
CREATE UNIQUE INDEX IF NOT EXISTS idx_buildings_code 
  ON buildings(building_code) WHERE building_code IS NOT NULL;
CREATE UNIQUE INDEX IF NOT EXISTS idx_units_code 
  ON units(unit_code) WHERE unit_code IS NOT NULL;

-- 3. Генерация кодов для существующих данных:
-- См. docs/uj-identifiers.md раздел "Миграция"
```

### Новые проекты:

Все коды генерируются **автоматически** при создании через UI или API.

---

## Breaking Changes

### v2.0.0
- **Изменён формат отображения:** Теперь все компоненты показывают **полные** идентификаторы
- **CompositionEditor:** `ZR01` → `UJ000001-ZR01`
- **Реестры:** `EF001` → `UJ000001-ZR01-EF001`

**Миграция:** Не требуется, обратная совместимость сохранена

### v1.0.0
- Добавлены новые поля в БД: требуется обновление схемы
- Новые зависимости в компонентах: требуется npm install

---

## Roadmap

### v2.1.0 - Расширенные функции (планируется)
- [ ] QR-коды для помещений
- [ ] Экспорт реестров с идентификаторами
- [ ] Bulk операции с кодами
- [ ] История изменений идентификаторов

### v2.2.0 - Интеграция (планируется)
- [ ] API endpoint для получения всех кодов
- [ ] Валидация при импорте
- [ ] Синхронизация с внешними системами
- [ ] Резервное копирование кодов

---

**Автор:** AI Assistant  
**Лицензия:** Проект reestr-mkd  
**Статус:** ✅ Production Ready
